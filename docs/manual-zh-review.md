# 手册中文审阅记录（仅本地保留）

当前 REST 修改已保存到 `local/rest-control-review`，本地提交 `578226dd`。手册分支 `local/manual-zh-review` 从原 dev 基点新建，恢复的是 `manual-before-rest-control-refactor` 中的手册文件，不包含 REST 重构。原 stash 保留；暂存中的旧计划移至本地 docs。

## 中文修订

通读八个章节，以中文读者为中心修订用词和句式：

- Manual → 用户手册；Releases → 更新日志。
- 抽屉 → 阅读设置面板；TUI 首次出现时说明为“终端界面”。
- “多种使用形态”“宿主能力”“手册边界”等开发者表述，改为具体的版本、功能和操作说明。
- 统一“自动切边”、桌面版、托盘版、命令行版，以及系统废纸篓或回收站等名称。
- 修正指定 8080 端口后仍访问默认 1234 的说明；明确反向代理须保留配置的路径前缀；区分远程文件系统 PDF 缓存与远程 ComiGo 代理。
- 中文页面的浏览器标题、顶部导航、辅助说明、章节导航和安装包架构名称均使用自然中文。API、路径、命令和文件名保留其准确拼写。

## 下载地址

三个语言版本的安装包链接与示例命令均使用 `https://comigo.xyz/yumenaka/comigo/releases/download/latest/`，文件名保留 `_latest_`。移除了直接 GitHub 安装、源码安装和固定旧版本下载指引。GitHub 项目、更新日志与问题反馈链接仍作为参考资料保留。

下载选择器中的 21 个不同安装包地址均以 GET 请求验证，返回 302 转发响应；未下载完整安装包。每种语言正文各有 11 个固定安装包地址，格式检查通过。

## 验证与文件状态

- 全量 Go 测试通过；手册路由和内嵌正文测试通过。
- 真实 Chromium 验证中文搜索、三种语言、复制与失败回退、锚点定位、菜单焦点、手机目录和五种视口宽度；没有页面脚本错误，也没有手册 SSE 连接。
- 浏览器逐一切换全部 21 个下载选项，检查固定链接。另查看中文安装页与手机首页截图，手机首页无横向溢出。
- `templ fmt ./templ && templ generate`、`bun run dev --no-cache` 和 `git diff --check` 通过。
- 源码为手册正文 JSON、页面级 JS/CSS、templ 页面、路由、侧栏入口及检查脚本；`*_templ.go` 和 `assets/dist/main.js` 为同步生成产物。dist 中 Alpine 版本字段由旧产物的 3.16.2 更新为当前依赖的 3.16.3。
- 手册修改保留在暂存区，未创建手册提交，也未推送。REST 审阅记录与本记录均不加入暂存区。
