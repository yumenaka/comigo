package settings_page

import (
	"github.com/yumenaka/comigo/htmx/state"
)
templ MainArea(s *state.GlobalState) {
    <div class="w-full h-full flex justify-center font-extrabold text-lg items-center bg-yellow-300 flex-1">MainArea</div>
}
